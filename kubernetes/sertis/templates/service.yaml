apiVersion: v1
kind: Service
metadata:
  name: {{ include "sertis.fullname" . }}
  labels:
    {{- include "sertis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sertis.selectorLabels" . | nindent 4 }}
