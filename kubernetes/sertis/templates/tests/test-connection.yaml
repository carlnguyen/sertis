apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sertis.fullname" . }}-test-connection"
  labels:
    {{- include "sertis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sertis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
